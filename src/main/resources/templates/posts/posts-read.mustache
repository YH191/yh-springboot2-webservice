{{>layout/header}}


<div class="col-md-12" align="center">
    <div class="col-md-4">
        <h1>게시글 읽기</h1>
        <form>
            <div class="form-group">
                <label for="title">글 번호</label>
                <input type="text" class="form-control"
                       id="id" value="{{posts.id}}" readonly>
            </div>t
            <div class="form-group">
                <label for="content"> 조회수 </label>
                <input type="text" class="form-control"
                       id="conent" value="{{posts.view}}" readonly>
            </div>
            <div class="form-group">
                <label for="title">제목</label>
                <input type="text" class="form-control"
                       id="title" value="{{posts.title}}" readonly>
            </div>
            <div class="form-group">
                <label for="author"> 작성자 </label>
                <input type="text" class="form-control"
                       id="author" value="{{posts.author}}" readonly>
            </div>
            <div class="form-group">
                <label for="content"> 내용 </label>
                <textarea class="form-control"
                          id="content" readonly>{{posts.content}}</textarea>
            </div>
        </form>
        <a href="/" role="button" class="btn btn-secondary">목록</a>
        {{! Buttons }}
        <a href="/posts/update/{{posts.id}}" role="button"
           class="btn btn-primary bi bi-pencil-square">수정</a>
        <button type="button" class="btn btn-danger"
                id="btn-delete">삭제</button>
    </div>
</div>
{{>layout/footer}}